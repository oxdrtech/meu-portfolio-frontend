services:
  app:
    build:
      context: .
      target: development
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    networks:
      - meuportfolio-front-network

networks:
  meuportfolio-front-network:
    driver: bridge
